"""Gateway 路由集合(详见 v2 plan §Phase A + Phase C fix)。

Phase A(9 路由):auth / books / llm / config / cost / doctor / notifications / style / write_proxy
Phase C fix:state —— 代理到 state-service 的 truth / snapshot
"""
